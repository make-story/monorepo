# 참고용
# https://heropy.blog/2019/01/31/node-js-npm-module-publish/
# NPM에 모듈을 배포하기 위해서는 사용자 등록(회원가입) 및 로그인 
# npm adduser or npm login
# $ npm adduser --registry https://private저장소URL
# Username:
# Password:
# Email:

# 환경변수 값을 통한 값 설정 방법 (젠킨스 잡 빌드)
# https://stackoverflow.com/questions/24143973/npm-adduser-via-bash
#export NPM_AUTH_TOKEN=
#export NPM_EMAIL=yu9221@gmail.com
#echo "_auth = $NPM_AUTH_TOKEN" > ~/.npmrc
#echo "email = $NPM_EMAIL" >> ~/.npmrc

# _auth 값 생성
# $ echo -n '<Username값>:<Password값>' | openssl base64

# https://docs.npmjs.com/cli/v6/using-npm/config
# 기본 저장소 URL값 : "http://registry.npmjs.org/"
#registry=http://registry.npmjs.org/
#registry=https://registry.npmjs.org/
#@test-scope:registry=http://nexus:8081/nexus/content/repositories/npm-test/

#always-auth=true
#email=yu9221@gmail.com
#_auth=