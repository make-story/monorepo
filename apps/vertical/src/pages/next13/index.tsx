/*import { use } from 'react';

export default function Page() {
  const data = use(getData());
  return (
    <>
      TEST<div>{JSON.stringify(data, null, 2)}</div>
    </>
  );
}

export async function getData() {
  const response = await fetch('/api');
  const data = await response.json();
  return data;
}
*/

export default function Page() {
  return <></>;
}
